// src/components/HomepageComponents/Hero.js
import React from 'react';

// We'll keep this as an empty component since we're moving the logo to Mission
export default function Hero() {
  return null;
}
